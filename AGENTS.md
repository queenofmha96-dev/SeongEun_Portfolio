# Game Sound Designer Portfolio - Project Guidelines & Rules

이 문서는 포트폴리오 웹사이트의 UI/UX 디자인 규칙, 반응형 원칙, 코드 컨벤션을 정의합니다. AI 에이전트는 모든 수정 및 기능 추가 작업 시 이 규칙을 항상 최우선으로 준수해야 합니다.

---

## 1. 반응형 분리 및 격리 원칙 (Mobile & Desktop Isolation)
모바일과 PC 화면은 서로의 레이아웃이나 가독성을 해치지 않도록 **철저히 분리/보존**되어야 합니다.

- **모바일 우선(Mobile-First) 점진적 확장**:
  - 기본 클래스는 모바일(Narrow 화면) 기준으로 선언하고, `sm:`, `md:`, `lg:`, `xl:` 접두사를 통해서만 데스크톱 전용 스타일을 확장합니다.
  - PC 레이아웃 수정 시 모바일 기본 클래스를 건드리지 않고 브레이크포인트(`md:`, `lg:`) 내에서만 수정합니다.
  - 모바일 레이아웃 수정 시 데스크톱 뷰포트에서 비정상적인 스트레칭이나 줄바꿈이 발생하지 않는지 반드시 양쪽을 교차 확인합니다.
- **터치 & 인터랙션 보장**:
  - 모바일 터치 타깃은 최소 44px 이상(`p-2.5` 이상 또는 명시적 높이) 확보.
  - 모바일에서 호버(`hover:`) 효과가 끈적하게 남지 않도록 터치 인터랙션 고려.
- **텍스트 줄바꿈 방어**:
  - 국문 텍스트는 어색한 단어 잘림을 방지하기 위해 `break-keep`을 기본 적용.
  - 뱃지/태그/날짜 등 짧은 라벨은 모바일에서도 절대 깨지지 않도록 `whitespace-nowrap`, `shrink-0` 적용.

---

## 2. 디자인 시스템 및 구분선 규칙 (Design System & Dividers)

### 2.1 톤 & 매너
- **테마**: 사이버펑크 & 프리미엄 게임 오디오 다크 캔버스
- **배경색**: 딥 네이비/다크 (`#0b0d18`, `#06070c`)
- **포인트 액센트**:
  - 시그니처/오디오/SFX: Cyan (`text-cyan-400`, `bg-cyan-950/90`, `border-cyan-500/50`)
  - 게임/경력/시간: Amber (`text-amber-400`, `bg-amber-950/90`, `border-amber-500/50`)
  - 상태/활성: Emerald (`text-emerald-400`, `bg-emerald-500`)

### 2.2 구분선(Divider) 및 여백(Spacing) 표준 규격
- **대단락 섹션 헤더 (`01, 02, 03, 04`)**:
  - `border-b border-slate-800/80 pb-3.5 sm:pb-4 font-mono`
  - 섹션 간 상하 여백: `space-y-12 sm:space-y-20`
- **섹션 내부 서브 블록 전환선**:
  - `border-t border-slate-800/80 pt-8 sm:pt-10`
- **서브 헤더 하단선**:
  - `border-b border-slate-800/70 pb-3`
- **이중선 및 과도한 박스 지양 (Anti-Slop)**:
  - 대단락 헤더 바로 아래에 불필요한 이중 구분선(`border-b`)을 중복 사용하지 않음.
  - 불필요하게 겹치는 다중 외곽 카드 박스(Card inside Card) 지양, 플랫하고 시원한 여백 기반 레이아웃 유지.

---

## 3. 프로필 & 이력(Profile & Career) 전용 규칙

- **프로필 상단 인트로**:
  - 미니멀 플랫 스타일 유지 (답답한 외곽 박스 테두리 지양).
  - 이름(`양성은 (SEONGEUN)`) 바로 아래에 이메일 및 위치 정보를 자연스럽게 연결.
  - `Game Sound Designer & Audio Artist` 같은 중복 직무 라벨 상자를 추가하지 않음.
- **경력 타임라인 (PROJECT & EXPERIENCE)**:
  - 날짜 뱃지(`[2024 - Present]`)는 오른쪽 끝으로 동떨어지게 분리하지 않고, 회사명/역할 바로 앞에 **`[기간 뱃지] 회사명 • 역할`** 순으로 자연스럽게 결합하여 한눈에 파악되도록 배치.
  - 모바일에서도 날짜 뱃지가 줄바꿈되지 않도록 `shrink-0`, `whitespace-nowrap` 유지.

---

## 4. 사운드 엔진 & 인터랙션 (Sound Engine)
- 브라우저 Web Audio API 기반 오디오 신디사이저 엔진(`soundEngine.ts`) 연동 유지.
- 버튼 호버(`playHover`), 탭 전환(`playClick`), SFX 랩 실시간 사운드 생성기 기능 보존.
